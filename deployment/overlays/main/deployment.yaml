apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  template:
    spec:
      containers:
        - name: toolbox
          imagePullPolicy: Always
      imagePullSecrets:
        - name: ocirsecret
